<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:jpa="http://www.springframework.org/schema/data/jpa"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/context 
	http://www.springframework.org/schema/context/spring-context.xsd
    http://www.springframework.org/schema/tx 
    http://www.springframework.org/schema/tx/spring-tx.xsd
    http://www.springframework.org/schema/aop 
	http://www.springframework.org/schema/aop/spring-aop.xsd
	http://www.springframework.org/schema/data/jpa
    http://www.springframework.org/schema/data/jpa/spring-jpa.xsd
    " default-autowire="byName">
 	<bean class="org.springframework.orm.jpa.support.PersistenceAnnotationBeanPostProcessor" />
		
	<aop:config>
		<aop:pointcut id="ChargeFacadePoint"
			expression="execution(* com.gateweb.charge.service.impl.ChargeFacadeImpl.*(..))" />
		<aop:advisor advice-ref="txAdviceCharge" pointcut-ref="ChargeFacadePoint" />
	</aop:config>
		
	<jpa:repositories base-package="com.gateweb.charge.repository" 
		entity-manager-factory-ref="entityManagerFactoryCharge"
		transaction-manager-ref="transactionManagerCharge" />

	<bean id="jpaDataSourceCharge" class="org.apache.commons.dbcp2.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${charge.jdbc.driverClassName}" />
		<property name="url" value="${charge.jdbc.url}" />
		<property name="username" value="${charge.jdbc.username}" />
		<property name="password" value="${charge.jdbc.password}" />
		<property name="maxTotal" value="${jdbc.maxTotal}" />
		<property name="initialSize" value="${jdbc.initialSize}" />
		<property name="maxIdle" value="${jdbc.maxIdle}" />
		<property name="maxWaitMillis" value="${jdbc.maxWaitMillis}" />
		<property name="logAbandoned" value="${jdbc.logAbandoned}" />
		<property name="removeAbandonedTimeout" value="${jdbc.removeAbandonedTimeout}" />
		<property name="removeAbandonedOnMaintenance" value="${jdbc.removeAbandonedOnMaintenance}" />
		<property name="defaultAutoCommit" value="${jdbc.defaultAutoCommit}" />
	</bean>

	<bean id="entityManagerFactoryCharge"
		class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="dataSource" ref="jpaDataSourceCharge" />
		<property name="jtaDataSource" ref="jpaDataSourceCharge" />
		<property name="persistenceUnitName" value="chargeFacade" />
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
				<property name="showSql" value="${jdbc.showSql}" />
				<property name="generateDdl" value="${jdbc.generateDdl}" />
				<property name="databasePlatform" value="${charge.jdbc.dialect}" />
			</bean>
		</property>
		<property name="jpaProperties">
	       <props>
	            <prop key="hibernate.hbm2ddl.auto">${jdbc.hbm2dll.auto}</prop>
	            <prop key="hibernate.default_schema">${jdbc.default.schema}</prop>
	       </props>
		</property>	
	</bean>

	<bean id="transactionManagerCharge" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="entityManagerFactoryCharge" />
		<property name="dataSource" ref="jpaDataSourceCharge" />
	</bean>

	<tx:advice id="txAdviceCharge" transaction-manager="transactionManagerCharge">
		<tx:attributes>
			<tx:method name="create*" propagation="REQUIRED" />
			<tx:method name="delete*" propagation="REQUIRED" />
			<tx:method name="update*" propagation="REQUIRED" />
			<tx:method name="revert*" propagation="REQUIRED" />
			<tx:method name="save*" propagation="REQUIRED" rollback-for="Throwable" />
			<tx:method name="send*" propagation="REQUIRED" rollback-for="Throwable" />
			<tx:method name="deprecate*" propagation="REQUIRED" rollback-for="Throwable" />						
			<tx:method name="*" propagation="REQUIRED" read-only="true" />			
		</tx:attributes>
	</tx:advice>

</beans>
