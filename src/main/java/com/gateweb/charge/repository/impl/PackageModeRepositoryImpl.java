/*
 * $Header: $
 * This java source file is generated by pkliu on Tue Jan 30 14:38:14 CST 2018
 * For more information, please contact pkliu@sysfoundry.com
 */
package com.gateweb.charge.repository.impl; 
import com.gateweb.charge.repository.*; 
import com.gateweb.charge.model.*;    
import com.gateweb.charge.*;

import com.meshinnovation.db.dao.exception.DaoSystemException;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import java.util.Collection;
import javax.persistence.Query;
//import org.springframework.orm.ObjectRetrievalFailureException;
import java.util.List;
import java.util.ArrayList;
import javax.persistence.*;

import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;
import javax.persistence.criteria.CriteriaQuery;
import org.springframework.stereotype.Repository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.domain.Pageable;

import com.google.common.collect.Lists;
import com.querydsl.core.BooleanBuilder;


/**
 * 
 * @author pkliu
 *
 * This class provides methods to populate DB Table of PackageMode
 */
//@Repository("packageModeRepositoryCustom")
public class PackageModeRepositoryImpl implements PackageModeRepositoryCustom {
	
	/**
	 * <p>
	 * <code>Log</code> instance for this application.
	 * </p>
	 */
	protected final Log log = LogFactory.getLog(getClass());
	
	/*	@PersistenceContext
	private EntityManager em;

	public void setEntityManager(EntityManager em) {
		this.em = em;
	}*/

	@Autowired
	PackageModeRepository packageModeRepository;
	
	public PackageModeRepositoryImpl(){
	}
	

	@SuppressWarnings("unchecked")
	public List<PackageModeEntity> searchWithVo(PackageModeEntity vo) {
		log.debug("PackageModeRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QPackageModeEntity packageModeEntity = QPackageModeEntity.packageModeEntity;

		if ( vo.getAdditionId() != null ) {
			builder.and(packageModeEntity.additionId.eq(vo.getAdditionId())); //java.lang.Integer
		}	
		if ( vo.getBroker2() != null && !"".equals(vo.getBroker2())) {
			builder.and(packageModeEntity.broker2.equalsIgnoreCase(vo.getBroker2())); //java.lang.String
		}
		if ( vo.getBrokerCp3() != null && !"".equals(vo.getBrokerCp3())) {
			builder.and(packageModeEntity.brokerCp3.equalsIgnoreCase(vo.getBrokerCp3())); //java.lang.String
		}
		if ( vo.getDealerCompanyId() != null ) {
			builder.and(packageModeEntity.dealerCompanyId.eq(vo.getDealerCompanyId())); //java.lang.Integer
		}	
		if ( vo.getBroker3() != null && !"".equals(vo.getBroker3())) {
			builder.and(packageModeEntity.broker3.equalsIgnoreCase(vo.getBroker3())); //java.lang.String
		}
		if ( vo.getCompanyId() != null ) {
			builder.and(packageModeEntity.companyId.eq(vo.getCompanyId())); //java.lang.Integer
		}	
		if ( vo.getPackageId() != null ) {
			builder.and(packageModeEntity.packageId.eq(vo.getPackageId())); //java.lang.Integer
		}	
		if ( vo.getPackageType() != null ) {
			builder.and(packageModeEntity.packageType.eq(vo.getPackageType())); //java.lang.Integer
		}	
		if ( vo.getChargeId() != null ) {
			builder.and(packageModeEntity.chargeId.eq(vo.getChargeId())); //java.lang.Integer
		}	
		if ( vo.getCreatorId() != null ) {
			builder.and(packageModeEntity.creatorId.eq(vo.getCreatorId())); //java.lang.Integer
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(packageModeEntity.modifierId.eq(vo.getModifierId())); //java.lang.Integer
		}	
		if ( vo.getCreateDate() != null ) {
			builder.and(packageModeEntity.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(packageModeEntity.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getDealerId() != null ) {
			builder.and(packageModeEntity.dealerId.eq(vo.getDealerId())); //java.lang.Integer
		}	
		if ( vo.getStatus() != null && !"".equals(vo.getStatus())) {
			builder.and(packageModeEntity.status.equalsIgnoreCase(vo.getStatus())); //java.lang.String
		}
		if ( vo.getBrokerCp2() != null && !"".equals(vo.getBrokerCp2())) {
			builder.and(packageModeEntity.brokerCp2.equalsIgnoreCase(vo.getBrokerCp2())); //java.lang.String
		}
		log.debug("PackageModeRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(packageModeRepository.findAll(builder.getValue()));	
	}
	
	@SuppressWarnings("unchecked")
	public List<PackageModeEntity> searchWithVo(PackageModeEntity vo, Pageable pageable) {
		log.debug("PackageModeRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QPackageModeEntity packageModeEntity = QPackageModeEntity.packageModeEntity;

		if ( vo.getAdditionId() != null ) {
			builder.and(packageModeEntity.additionId.eq(vo.getAdditionId())); //java.lang.Integer
		}	
		if ( vo.getBroker2() != null && !"".equals(vo.getBroker2())) {
			builder.and(packageModeEntity.broker2.equalsIgnoreCase(vo.getBroker2())); //java.lang.String
		}
		if ( vo.getBrokerCp3() != null && !"".equals(vo.getBrokerCp3())) {
			builder.and(packageModeEntity.brokerCp3.equalsIgnoreCase(vo.getBrokerCp3())); //java.lang.String
		}
		if ( vo.getDealerCompanyId() != null ) {
			builder.and(packageModeEntity.dealerCompanyId.eq(vo.getDealerCompanyId())); //java.lang.Integer
		}	
		if ( vo.getBroker3() != null && !"".equals(vo.getBroker3())) {
			builder.and(packageModeEntity.broker3.equalsIgnoreCase(vo.getBroker3())); //java.lang.String
		}
		if ( vo.getCompanyId() != null ) {
			builder.and(packageModeEntity.companyId.eq(vo.getCompanyId())); //java.lang.Integer
		}	
		if ( vo.getPackageId() != null ) {
			builder.and(packageModeEntity.packageId.eq(vo.getPackageId())); //java.lang.Integer
		}	
		if ( vo.getPackageType() != null ) {
			builder.and(packageModeEntity.packageType.eq(vo.getPackageType())); //java.lang.Integer
		}	
		if ( vo.getChargeId() != null ) {
			builder.and(packageModeEntity.chargeId.eq(vo.getChargeId())); //java.lang.Integer
		}	
		if ( vo.getCreatorId() != null ) {
			builder.and(packageModeEntity.creatorId.eq(vo.getCreatorId())); //java.lang.Integer
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(packageModeEntity.modifierId.eq(vo.getModifierId())); //java.lang.Integer
		}	
		if ( vo.getCreateDate() != null ) {
			builder.and(packageModeEntity.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(packageModeEntity.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getDealerId() != null ) {
			builder.and(packageModeEntity.dealerId.eq(vo.getDealerId())); //java.lang.Integer
		}	
		if ( vo.getStatus() != null && !"".equals(vo.getStatus())) {
			builder.and(packageModeEntity.status.equalsIgnoreCase(vo.getStatus())); //java.lang.String
		}
		if ( vo.getBrokerCp2() != null && !"".equals(vo.getBrokerCp2())) {
			builder.and(packageModeEntity.brokerCp2.equalsIgnoreCase(vo.getBrokerCp2())); //java.lang.String
		}
		log.debug("PackageModeRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(packageModeRepository.findAll(builder.getValue(), pageable));	
	}
	
	@SuppressWarnings("unchecked")
	public List<PackageModeEntity> searchWithVo(PackageModeEntity vo, int pageOffset, int pageSize) {
		Pageable pageable = new PageRequest(pageOffset, pageSize);
		return searchWithVo(vo, pageable);
	}
	
	@SuppressWarnings("unchecked")
	public List<PackageModeEntity> searchLikeVo(PackageModeEntity vo) {
		log.debug("PackageModeRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QPackageModeEntity packageModeEntity = QPackageModeEntity.packageModeEntity;

		if ( vo.getAdditionId() != null ) {
			builder.and(packageModeEntity.additionId.eq(vo.getAdditionId())); //java.lang.Integer
		}	
		if ( vo.getBroker2() != null && !"".equals(vo.getBroker2())) {
			builder.and(packageModeEntity.broker2.containsIgnoreCase(vo.getBroker2())); //java.lang.String
		}
		if ( vo.getBrokerCp3() != null && !"".equals(vo.getBrokerCp3())) {
			builder.and(packageModeEntity.brokerCp3.containsIgnoreCase(vo.getBrokerCp3())); //java.lang.String
		}
		if ( vo.getDealerCompanyId() != null ) {
			builder.and(packageModeEntity.dealerCompanyId.eq(vo.getDealerCompanyId())); //java.lang.Integer
		}	
		if ( vo.getBroker3() != null && !"".equals(vo.getBroker3())) {
			builder.and(packageModeEntity.broker3.containsIgnoreCase(vo.getBroker3())); //java.lang.String
		}
		if ( vo.getCompanyId() != null ) {
			builder.and(packageModeEntity.companyId.eq(vo.getCompanyId())); //java.lang.Integer
		}	
		if ( vo.getPackageId() != null ) {
			builder.and(packageModeEntity.packageId.eq(vo.getPackageId())); //java.lang.Integer
		}	
		if ( vo.getPackageType() != null ) {
			builder.and(packageModeEntity.packageType.eq(vo.getPackageType())); //java.lang.Integer
		}	
		if ( vo.getChargeId() != null ) {
			builder.and(packageModeEntity.chargeId.eq(vo.getChargeId())); //java.lang.Integer
		}	
		if ( vo.getCreatorId() != null ) {
			builder.and(packageModeEntity.creatorId.eq(vo.getCreatorId())); //java.lang.Integer
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(packageModeEntity.modifierId.eq(vo.getModifierId())); //java.lang.Integer
		}	
		if ( vo.getCreateDate() != null ) {
			builder.and(packageModeEntity.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(packageModeEntity.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getDealerId() != null ) {
			builder.and(packageModeEntity.dealerId.eq(vo.getDealerId())); //java.lang.Integer
		}	
		if ( vo.getStatus() != null && !"".equals(vo.getStatus())) {
			builder.and(packageModeEntity.status.containsIgnoreCase(vo.getStatus())); //java.lang.String
		}
		if ( vo.getBrokerCp2() != null && !"".equals(vo.getBrokerCp2())) {
			builder.and(packageModeEntity.brokerCp2.containsIgnoreCase(vo.getBrokerCp2())); //java.lang.String
		}
		log.debug("PackageModeRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(packageModeRepository.findAll(builder.getValue()));
	}		
	
	

	@SuppressWarnings("unchecked")
	public List<PackageModeEntity> searchLikeVo(PackageModeEntity vo, Pageable pageable) {
		log.debug("PackageModeRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QPackageModeEntity packageModeEntity = QPackageModeEntity.packageModeEntity;

		if ( vo.getAdditionId() != null ) {
			builder.and(packageModeEntity.additionId.eq(vo.getAdditionId())); //java.lang.Integer
		}	
		if ( vo.getBroker2() != null && !"".equals(vo.getBroker2())) {
			builder.and(packageModeEntity.broker2.containsIgnoreCase(vo.getBroker2())); //java.lang.String
		}
		if ( vo.getBrokerCp3() != null && !"".equals(vo.getBrokerCp3())) {
			builder.and(packageModeEntity.brokerCp3.containsIgnoreCase(vo.getBrokerCp3())); //java.lang.String
		}
		if ( vo.getDealerCompanyId() != null ) {
			builder.and(packageModeEntity.dealerCompanyId.eq(vo.getDealerCompanyId())); //java.lang.Integer
		}	
		if ( vo.getBroker3() != null && !"".equals(vo.getBroker3())) {
			builder.and(packageModeEntity.broker3.containsIgnoreCase(vo.getBroker3())); //java.lang.String
		}
		if ( vo.getCompanyId() != null ) {
			builder.and(packageModeEntity.companyId.eq(vo.getCompanyId())); //java.lang.Integer
		}	
		if ( vo.getPackageId() != null ) {
			builder.and(packageModeEntity.packageId.eq(vo.getPackageId())); //java.lang.Integer
		}	
		if ( vo.getPackageType() != null ) {
			builder.and(packageModeEntity.packageType.eq(vo.getPackageType())); //java.lang.Integer
		}	
		if ( vo.getChargeId() != null ) {
			builder.and(packageModeEntity.chargeId.eq(vo.getChargeId())); //java.lang.Integer
		}	
		if ( vo.getCreatorId() != null ) {
			builder.and(packageModeEntity.creatorId.eq(vo.getCreatorId())); //java.lang.Integer
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(packageModeEntity.modifierId.eq(vo.getModifierId())); //java.lang.Integer
		}	
		if ( vo.getCreateDate() != null ) {
			builder.and(packageModeEntity.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(packageModeEntity.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getDealerId() != null ) {
			builder.and(packageModeEntity.dealerId.eq(vo.getDealerId())); //java.lang.Integer
		}	
		if ( vo.getStatus() != null && !"".equals(vo.getStatus())) {
			builder.and(packageModeEntity.status.containsIgnoreCase(vo.getStatus())); //java.lang.String
		}
		if ( vo.getBrokerCp2() != null && !"".equals(vo.getBrokerCp2())) {
			builder.and(packageModeEntity.brokerCp2.containsIgnoreCase(vo.getBrokerCp2())); //java.lang.String
		}
		log.debug("PackageModeRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(packageModeRepository.findAll(builder.getValue(), pageable));
	}	
	
	@SuppressWarnings("unchecked")
	public List<PackageModeEntity> searchLikeVo(PackageModeEntity vo, int pageOffset, int pageSize) {
		Pageable pageable = new PageRequest(pageOffset, pageSize);
		return searchLikeVo(vo, pageable);
	}
	
}
