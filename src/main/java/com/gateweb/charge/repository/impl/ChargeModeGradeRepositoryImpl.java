/*
 * $Header: $
 * This java source file is generated by pkliu on Tue Jan 30 14:38:14 CST 2018
 * For more information, please contact pkliu@sysfoundry.com
 */
package com.gateweb.charge.repository.impl; 
import com.gateweb.charge.repository.*; 
import com.gateweb.charge.model.*;    
import com.gateweb.charge.*;

import com.meshinnovation.db.dao.exception.DaoSystemException;
import org.apache.logging.log4j.Logger;
import org.apache.logging.log4j.LogManager;
import java.util.Collection;
import javax.persistence.Query;
//import org.springframework.orm.ObjectRetrievalFailureException;
import java.util.List;
import java.util.ArrayList;
import javax.persistence.*;

import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;
import javax.persistence.criteria.CriteriaQuery;
import org.springframework.stereotype.Repository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.domain.Pageable;

import com.google.common.collect.Lists;
import com.querydsl.core.BooleanBuilder;


/**
 * 
 * @author pkliu
 *
 * This class provides methods to populate DB Table of ChargeModeGrade
 */
//@Repository("chargeModeGradeRepositoryCustom")
public class ChargeModeGradeRepositoryImpl implements ChargeModeGradeRepositoryCustom {
	
	/**
	 * <p>
	 * <code>Log</code> instance for this application.
	 * </p>
	 */
	protected final Logger logger = LogManager.getLogger(getClass());
	
	/*	@PersistenceContext
	private EntityManager em;

	public void setEntityManager(EntityManager em) {
		this.em = em;
	}*/

	@Autowired
	ChargeModeGradeRepository chargeModeGradeRepository;
	
	public ChargeModeGradeRepositoryImpl(){
	}
	

	@SuppressWarnings("unchecked")
	public List<ChargeModeGradeEntity> searchWithVo(ChargeModeGradeEntity vo) {
		logger.debug("ChargeModeGradeRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QChargeModeGradeEntity chargeModeGradeEntity = QChargeModeGradeEntity.chargeModeGradeEntity;

		if ( vo.getBaseQuantity() != null ) {
			builder.and(chargeModeGradeEntity.baseQuantity.eq(vo.getBaseQuantity())); //java.lang.Integer
		}	
		if ( vo.getChargeCycle() != null ) {
			builder.and(chargeModeGradeEntity.chargeCycle.eq(vo.getChargeCycle())); //java.lang.Integer
		}	
		if ( vo.getPrePayment() != null ) {
			builder.and(chargeModeGradeEntity.prePayment.eq(vo.getPrePayment())); //java.lang.Integer
		}	
		if ( vo.getGradeCnt() != null ) {
			builder.and(chargeModeGradeEntity.gradeCnt.eq(vo.getGradeCnt())); //java.lang.Integer
		}	
		if ( vo.getGradePrice() != null ) {
			builder.and(chargeModeGradeEntity.gradePrice.eq(vo.getGradePrice())); //java.lang.Integer
		}	
		if ( vo.getFreeMonth() != null ) {
			builder.and(chargeModeGradeEntity.freeMonth.eq(vo.getFreeMonth())); //java.lang.Integer
		}	
		if ( vo.getChargeId() != null ) {
			builder.and(chargeModeGradeEntity.chargeId.eq(vo.getChargeId())); //java.lang.Integer
		}	
		if ( vo.getFeePeriod() != null ) {
			builder.and(chargeModeGradeEntity.feePeriod.eq(vo.getFeePeriod())); //java.lang.Integer
		}	
		if ( vo.getPackageName() != null && !"".equals(vo.getPackageName())) {
			builder.and(chargeModeGradeEntity.packageName.equalsIgnoreCase(vo.getPackageName())); //java.lang.String
		}
		if ( vo.getSalesPrice() != null ) {
			builder.and(chargeModeGradeEntity.salesPrice.eq(vo.getSalesPrice())); //java.lang.Integer
		}	
		if ( vo.getCreatorId() != null ) {
			builder.and(chargeModeGradeEntity.creatorId.eq(vo.getCreatorId())); //java.lang.Integer
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(chargeModeGradeEntity.modifierId.eq(vo.getModifierId())); //java.lang.Integer
		}	
		if ( vo.getContractLimit() != null ) {
			builder.and(chargeModeGradeEntity.contractLimit.eq(vo.getContractLimit())); //java.lang.Integer
		}	
		if ( vo.getHasGrade() != null && !"".equals(vo.getHasGrade())) {
			builder.and(chargeModeGradeEntity.hasGrade.equalsIgnoreCase(vo.getHasGrade())); //java.lang.String
		}
		if ( vo.getCreateDate() != null ) {
			builder.and(chargeModeGradeEntity.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(chargeModeGradeEntity.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getStatus() != null ) {
			builder.and(chargeModeGradeEntity.status.eq(vo.getStatus())); //java.lang.Integer
		}	
		logger.debug("ChargeModeGradeRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(chargeModeGradeRepository.findAll(builder.getValue()));	
	}
	
	@SuppressWarnings("unchecked")
	public List<ChargeModeGradeEntity> searchWithVo(ChargeModeGradeEntity vo, Pageable pageable) {
		logger.debug("ChargeModeGradeRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QChargeModeGradeEntity chargeModeGradeEntity = QChargeModeGradeEntity.chargeModeGradeEntity;

		if ( vo.getBaseQuantity() != null ) {
			builder.and(chargeModeGradeEntity.baseQuantity.eq(vo.getBaseQuantity())); //java.lang.Integer
		}	
		if ( vo.getChargeCycle() != null ) {
			builder.and(chargeModeGradeEntity.chargeCycle.eq(vo.getChargeCycle())); //java.lang.Integer
		}	
		if ( vo.getPrePayment() != null ) {
			builder.and(chargeModeGradeEntity.prePayment.eq(vo.getPrePayment())); //java.lang.Integer
		}	
		if ( vo.getGradeCnt() != null ) {
			builder.and(chargeModeGradeEntity.gradeCnt.eq(vo.getGradeCnt())); //java.lang.Integer
		}	
		if ( vo.getGradePrice() != null ) {
			builder.and(chargeModeGradeEntity.gradePrice.eq(vo.getGradePrice())); //java.lang.Integer
		}	
		if ( vo.getFreeMonth() != null ) {
			builder.and(chargeModeGradeEntity.freeMonth.eq(vo.getFreeMonth())); //java.lang.Integer
		}	
		if ( vo.getChargeId() != null ) {
			builder.and(chargeModeGradeEntity.chargeId.eq(vo.getChargeId())); //java.lang.Integer
		}	
		if ( vo.getFeePeriod() != null ) {
			builder.and(chargeModeGradeEntity.feePeriod.eq(vo.getFeePeriod())); //java.lang.Integer
		}	
		if ( vo.getPackageName() != null && !"".equals(vo.getPackageName())) {
			builder.and(chargeModeGradeEntity.packageName.equalsIgnoreCase(vo.getPackageName())); //java.lang.String
		}
		if ( vo.getSalesPrice() != null ) {
			builder.and(chargeModeGradeEntity.salesPrice.eq(vo.getSalesPrice())); //java.lang.Integer
		}	
		if ( vo.getCreatorId() != null ) {
			builder.and(chargeModeGradeEntity.creatorId.eq(vo.getCreatorId())); //java.lang.Integer
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(chargeModeGradeEntity.modifierId.eq(vo.getModifierId())); //java.lang.Integer
		}	
		if ( vo.getContractLimit() != null ) {
			builder.and(chargeModeGradeEntity.contractLimit.eq(vo.getContractLimit())); //java.lang.Integer
		}	
		if ( vo.getHasGrade() != null && !"".equals(vo.getHasGrade())) {
			builder.and(chargeModeGradeEntity.hasGrade.equalsIgnoreCase(vo.getHasGrade())); //java.lang.String
		}
		if ( vo.getCreateDate() != null ) {
			builder.and(chargeModeGradeEntity.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(chargeModeGradeEntity.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getStatus() != null ) {
			builder.and(chargeModeGradeEntity.status.eq(vo.getStatus())); //java.lang.Integer
		}	
		logger.debug("ChargeModeGradeRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(chargeModeGradeRepository.findAll(builder.getValue(), pageable));	
	}
	
	@SuppressWarnings("unchecked")
	public List<ChargeModeGradeEntity> searchWithVo(ChargeModeGradeEntity vo, int pageOffset, int pageSize) {
		Pageable pageable = new PageRequest(pageOffset, pageSize);
		return searchWithVo(vo, pageable);
	}
	
	@SuppressWarnings("unchecked")
	public List<ChargeModeGradeEntity> searchLikeVo(ChargeModeGradeEntity vo) {
		logger.debug("ChargeModeGradeRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QChargeModeGradeEntity chargeModeGradeEntity = QChargeModeGradeEntity.chargeModeGradeEntity;

		if ( vo.getBaseQuantity() != null ) {
			builder.and(chargeModeGradeEntity.baseQuantity.eq(vo.getBaseQuantity())); //java.lang.Integer
		}	
		if ( vo.getChargeCycle() != null ) {
			builder.and(chargeModeGradeEntity.chargeCycle.eq(vo.getChargeCycle())); //java.lang.Integer
		}	
		if ( vo.getPrePayment() != null ) {
			builder.and(chargeModeGradeEntity.prePayment.eq(vo.getPrePayment())); //java.lang.Integer
		}	
		if ( vo.getGradeCnt() != null ) {
			builder.and(chargeModeGradeEntity.gradeCnt.eq(vo.getGradeCnt())); //java.lang.Integer
		}	
		if ( vo.getGradePrice() != null ) {
			builder.and(chargeModeGradeEntity.gradePrice.eq(vo.getGradePrice())); //java.lang.Integer
		}	
		if ( vo.getFreeMonth() != null ) {
			builder.and(chargeModeGradeEntity.freeMonth.eq(vo.getFreeMonth())); //java.lang.Integer
		}	
		if ( vo.getChargeId() != null ) {
			builder.and(chargeModeGradeEntity.chargeId.eq(vo.getChargeId())); //java.lang.Integer
		}	
		if ( vo.getFeePeriod() != null ) {
			builder.and(chargeModeGradeEntity.feePeriod.eq(vo.getFeePeriod())); //java.lang.Integer
		}	
		if ( vo.getPackageName() != null && !"".equals(vo.getPackageName())) {
			builder.and(chargeModeGradeEntity.packageName.containsIgnoreCase(vo.getPackageName())); //java.lang.String
		}
		if ( vo.getSalesPrice() != null ) {
			builder.and(chargeModeGradeEntity.salesPrice.eq(vo.getSalesPrice())); //java.lang.Integer
		}	
		if ( vo.getCreatorId() != null ) {
			builder.and(chargeModeGradeEntity.creatorId.eq(vo.getCreatorId())); //java.lang.Integer
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(chargeModeGradeEntity.modifierId.eq(vo.getModifierId())); //java.lang.Integer
		}	
		if ( vo.getContractLimit() != null ) {
			builder.and(chargeModeGradeEntity.contractLimit.eq(vo.getContractLimit())); //java.lang.Integer
		}	
		if ( vo.getHasGrade() != null && !"".equals(vo.getHasGrade())) {
			builder.and(chargeModeGradeEntity.hasGrade.containsIgnoreCase(vo.getHasGrade())); //java.lang.String
		}
		if ( vo.getCreateDate() != null ) {
			builder.and(chargeModeGradeEntity.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(chargeModeGradeEntity.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getStatus() != null ) {
			builder.and(chargeModeGradeEntity.status.eq(vo.getStatus())); //java.lang.Integer
		}	
		logger.debug("ChargeModeGradeRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(chargeModeGradeRepository.findAll(builder.getValue()));
	}		
	
	

	@SuppressWarnings("unchecked")
	public List<ChargeModeGradeEntity> searchLikeVo(ChargeModeGradeEntity vo, Pageable pageable) {
		logger.debug("ChargeModeGradeRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QChargeModeGradeEntity chargeModeGradeEntity = QChargeModeGradeEntity.chargeModeGradeEntity;

		if ( vo.getBaseQuantity() != null ) {
			builder.and(chargeModeGradeEntity.baseQuantity.eq(vo.getBaseQuantity())); //java.lang.Integer
		}	
		if ( vo.getChargeCycle() != null ) {
			builder.and(chargeModeGradeEntity.chargeCycle.eq(vo.getChargeCycle())); //java.lang.Integer
		}	
		if ( vo.getPrePayment() != null ) {
			builder.and(chargeModeGradeEntity.prePayment.eq(vo.getPrePayment())); //java.lang.Integer
		}	
		if ( vo.getGradeCnt() != null ) {
			builder.and(chargeModeGradeEntity.gradeCnt.eq(vo.getGradeCnt())); //java.lang.Integer
		}	
		if ( vo.getGradePrice() != null ) {
			builder.and(chargeModeGradeEntity.gradePrice.eq(vo.getGradePrice())); //java.lang.Integer
		}	
		if ( vo.getFreeMonth() != null ) {
			builder.and(chargeModeGradeEntity.freeMonth.eq(vo.getFreeMonth())); //java.lang.Integer
		}	
		if ( vo.getChargeId() != null ) {
			builder.and(chargeModeGradeEntity.chargeId.eq(vo.getChargeId())); //java.lang.Integer
		}	
		if ( vo.getFeePeriod() != null ) {
			builder.and(chargeModeGradeEntity.feePeriod.eq(vo.getFeePeriod())); //java.lang.Integer
		}	
		if ( vo.getPackageName() != null && !"".equals(vo.getPackageName())) {
			builder.and(chargeModeGradeEntity.packageName.containsIgnoreCase(vo.getPackageName())); //java.lang.String
		}
		if ( vo.getSalesPrice() != null ) {
			builder.and(chargeModeGradeEntity.salesPrice.eq(vo.getSalesPrice())); //java.lang.Integer
		}	
		if ( vo.getCreatorId() != null ) {
			builder.and(chargeModeGradeEntity.creatorId.eq(vo.getCreatorId())); //java.lang.Integer
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(chargeModeGradeEntity.modifierId.eq(vo.getModifierId())); //java.lang.Integer
		}	
		if ( vo.getContractLimit() != null ) {
			builder.and(chargeModeGradeEntity.contractLimit.eq(vo.getContractLimit())); //java.lang.Integer
		}	
		if ( vo.getHasGrade() != null && !"".equals(vo.getHasGrade())) {
			builder.and(chargeModeGradeEntity.hasGrade.containsIgnoreCase(vo.getHasGrade())); //java.lang.String
		}
		if ( vo.getCreateDate() != null ) {
			builder.and(chargeModeGradeEntity.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(chargeModeGradeEntity.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getStatus() != null ) {
			builder.and(chargeModeGradeEntity.status.eq(vo.getStatus())); //java.lang.Integer
		}	
		logger.debug("ChargeModeGradeRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(chargeModeGradeRepository.findAll(builder.getValue(), pageable));
	}	
	
	@SuppressWarnings("unchecked")
	public List<ChargeModeGradeEntity> searchLikeVo(ChargeModeGradeEntity vo, int pageOffset, int pageSize) {
		Pageable pageable = new PageRequest(pageOffset, pageSize);
		return searchLikeVo(vo, pageable);
	}
	
}
