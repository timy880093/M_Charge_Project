/*
 * $Header: $
 * This java source file is generated by pkliu on Thu Aug 03 11:22:57 CST 2017
 * For more information, please contact pkliu@sysfoundry.com
 */
package com.gateweb.charge.repository.impl; 
//import org.springframework.orm.ObjectRetrievalFailureException;
import java.util.List;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.domain.Pageable;

import com.gateweb.charge.model.QUser;
import com.gateweb.charge.model.User;
import com.gateweb.charge.repository.User2Repository;
import com.gateweb.charge.repository.User2RepositoryCustom;
import com.google.common.collect.Lists;
import com.querydsl.core.BooleanBuilder;


/**
 * 
 * @author pkliu
 *
 * This class provides methods to populate DB Table of User
 */
//@Repository("user2RepositoryCustom")
public class User2RepositoryImpl implements User2RepositoryCustom {
	
	/**
	 * <p>
	 * <code>Log</code> instance for this application.
	 * </p>
	 */
	protected final Log log = LogFactory.getLog(getClass());
	
	/*	@PersistenceContext
	private EntityManager em;

	public void setEntityManager(EntityManager em) {
		this.em = em;
	}*/

	@Autowired
	User2Repository user2Repository;
	
	public User2RepositoryImpl(){
	}
	

	@SuppressWarnings("unchecked")
	public List<User> searchWithVo(User vo) {
		log.debug("UserRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QUser user = QUser.user;

		if ( vo.getCompanyId() != null ) {
			builder.and(user.companyId.eq(vo.getCompanyId())); //java.lang.Long
		}	
		if ( vo.getAuthKey() != null && !"".equals(vo.getAuthKey())) {
			builder.and(user.authKey.equalsIgnoreCase(vo.getAuthKey())); //java.lang.String
		}
		if ( vo.getDefaultTaxType() != null && !"".equals(vo.getDefaultTaxType())) {
			builder.and(user.defaultTaxType.equalsIgnoreCase(vo.getDefaultTaxType())); //java.lang.String
		}
		if ( vo.getDefaultB2bFlag() != null && !"".equals(vo.getDefaultB2bFlag())) {
			builder.and(user.defaultB2bFlag.equalsIgnoreCase(vo.getDefaultB2bFlag())); //java.lang.String
		}
		if ( vo.getPassword() != null && !"".equals(vo.getPassword())) {
			builder.and(user.password.equalsIgnoreCase(vo.getPassword())); //java.lang.String
		}
		if ( vo.getUserId() != null ) {
			builder.and(user.userId.eq(vo.getUserId())); //java.lang.Long
		}	
		if ( vo.getRoleId() != null ) {
			builder.and(user.roleId.eq(vo.getRoleId())); //java.lang.Long
		}	
		if ( vo.getName() != null && !"".equals(vo.getName())) {
			builder.and(user.name.equalsIgnoreCase(vo.getName())); //java.lang.String
		}
		if ( vo.getCreatorId() != null ) {
			builder.and(user.creatorId.eq(vo.getCreatorId())); //java.lang.Long
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(user.modifierId.eq(vo.getModifierId())); //java.lang.Long
		}	
		if ( vo.getLogoutTime() != null ) {
			builder.and(user.logoutTime.eq(vo.getLogoutTime())); //java.lang.Long
		}	
		if ( vo.getPrinterId() != null ) {
			builder.and(user.printerId.eq(vo.getPrinterId())); //java.lang.Long
		}	
		if ( vo.getAuthUrl() != null && !"".equals(vo.getAuthUrl())) {
			builder.and(user.authUrl.equalsIgnoreCase(vo.getAuthUrl())); //java.lang.String
		}
		if ( vo.getCreateDate() != null ) {
			builder.and(user.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getClose() != null ) {
			builder.and(user.close.eq(vo.getClose())); //java.lang.Boolean
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(user.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getAccount() != null && !"".equals(vo.getAccount())) {
			builder.and(user.account.equalsIgnoreCase(vo.getAccount())); //java.lang.String
		}
		if ( vo.getEmail() != null && !"".equals(vo.getEmail())) {
			builder.and(user.email.equalsIgnoreCase(vo.getEmail())); //java.lang.String
		}
		log.debug("UserRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(user2Repository.findAll(builder.getValue()));	
	}
	
	@SuppressWarnings("unchecked")
	public List<User> searchWithVo(User vo, Pageable pageable) {
		log.debug("UserRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QUser user = QUser.user;

		if ( vo.getCompanyId() != null ) {
			builder.and(user.companyId.eq(vo.getCompanyId())); //java.lang.Long
		}	
		if ( vo.getAuthKey() != null && !"".equals(vo.getAuthKey())) {
			builder.and(user.authKey.equalsIgnoreCase(vo.getAuthKey())); //java.lang.String
		}
		if ( vo.getDefaultTaxType() != null && !"".equals(vo.getDefaultTaxType())) {
			builder.and(user.defaultTaxType.equalsIgnoreCase(vo.getDefaultTaxType())); //java.lang.String
		}
		if ( vo.getDefaultB2bFlag() != null && !"".equals(vo.getDefaultB2bFlag())) {
			builder.and(user.defaultB2bFlag.equalsIgnoreCase(vo.getDefaultB2bFlag())); //java.lang.String
		}
		if ( vo.getPassword() != null && !"".equals(vo.getPassword())) {
			builder.and(user.password.equalsIgnoreCase(vo.getPassword())); //java.lang.String
		}
		if ( vo.getUserId() != null ) {
			builder.and(user.userId.eq(vo.getUserId())); //java.lang.Long
		}	
		if ( vo.getRoleId() != null ) {
			builder.and(user.roleId.eq(vo.getRoleId())); //java.lang.Long
		}	
		if ( vo.getName() != null && !"".equals(vo.getName())) {
			builder.and(user.name.equalsIgnoreCase(vo.getName())); //java.lang.String
		}
		if ( vo.getCreatorId() != null ) {
			builder.and(user.creatorId.eq(vo.getCreatorId())); //java.lang.Long
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(user.modifierId.eq(vo.getModifierId())); //java.lang.Long
		}	
		if ( vo.getLogoutTime() != null ) {
			builder.and(user.logoutTime.eq(vo.getLogoutTime())); //java.lang.Long
		}	
		if ( vo.getPrinterId() != null ) {
			builder.and(user.printerId.eq(vo.getPrinterId())); //java.lang.Long
		}	
		if ( vo.getAuthUrl() != null && !"".equals(vo.getAuthUrl())) {
			builder.and(user.authUrl.equalsIgnoreCase(vo.getAuthUrl())); //java.lang.String
		}
		if ( vo.getCreateDate() != null ) {
			builder.and(user.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getClose() != null ) {
			builder.and(user.close.eq(vo.getClose())); //java.lang.Boolean
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(user.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getAccount() != null && !"".equals(vo.getAccount())) {
			builder.and(user.account.equalsIgnoreCase(vo.getAccount())); //java.lang.String
		}
		if ( vo.getEmail() != null && !"".equals(vo.getEmail())) {
			builder.and(user.email.equalsIgnoreCase(vo.getEmail())); //java.lang.String
		}
		log.debug("UserRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(user2Repository.findAll(builder.getValue(), pageable));	
	}
	
	@SuppressWarnings("unchecked")
	public List<User> searchWithVo(User vo, int pageOffset, int pageSize) {
		Pageable pageable = new PageRequest(pageOffset, pageSize);
		return searchWithVo(vo, pageable);
	}
	
	@SuppressWarnings("unchecked")
	public List<User> searchLikeVo(User vo) {
		log.debug("UserRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QUser user = QUser.user;

		if ( vo.getCompanyId() != null ) {
			builder.and(user.companyId.eq(vo.getCompanyId())); //java.lang.Long
		}	
		if ( vo.getAuthKey() != null && !"".equals(vo.getAuthKey())) {
			builder.and(user.authKey.containsIgnoreCase(vo.getAuthKey())); //java.lang.String
		}
		if ( vo.getDefaultTaxType() != null && !"".equals(vo.getDefaultTaxType())) {
			builder.and(user.defaultTaxType.containsIgnoreCase(vo.getDefaultTaxType())); //java.lang.String
		}
		if ( vo.getDefaultB2bFlag() != null && !"".equals(vo.getDefaultB2bFlag())) {
			builder.and(user.defaultB2bFlag.containsIgnoreCase(vo.getDefaultB2bFlag())); //java.lang.String
		}
		if ( vo.getPassword() != null && !"".equals(vo.getPassword())) {
			builder.and(user.password.containsIgnoreCase(vo.getPassword())); //java.lang.String
		}
		if ( vo.getUserId() != null ) {
			builder.and(user.userId.eq(vo.getUserId())); //java.lang.Long
		}	
		if ( vo.getRoleId() != null ) {
			builder.and(user.roleId.eq(vo.getRoleId())); //java.lang.Long
		}	
		if ( vo.getName() != null && !"".equals(vo.getName())) {
			builder.and(user.name.containsIgnoreCase(vo.getName())); //java.lang.String
		}
		if ( vo.getCreatorId() != null ) {
			builder.and(user.creatorId.eq(vo.getCreatorId())); //java.lang.Long
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(user.modifierId.eq(vo.getModifierId())); //java.lang.Long
		}	
		if ( vo.getLogoutTime() != null ) {
			builder.and(user.logoutTime.eq(vo.getLogoutTime())); //java.lang.Long
		}	
		if ( vo.getPrinterId() != null ) {
			builder.and(user.printerId.eq(vo.getPrinterId())); //java.lang.Long
		}	
		if ( vo.getAuthUrl() != null && !"".equals(vo.getAuthUrl())) {
			builder.and(user.authUrl.containsIgnoreCase(vo.getAuthUrl())); //java.lang.String
		}
		if ( vo.getCreateDate() != null ) {
			builder.and(user.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getClose() != null ) {
			builder.and(user.close.eq(vo.getClose())); //java.lang.Boolean
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(user.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getAccount() != null && !"".equals(vo.getAccount())) {
			builder.and(user.account.containsIgnoreCase(vo.getAccount())); //java.lang.String
		}
		if ( vo.getEmail() != null && !"".equals(vo.getEmail())) {
			builder.and(user.email.containsIgnoreCase(vo.getEmail())); //java.lang.String
		}
		log.debug("UserRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(user2Repository.findAll(builder.getValue()));
	}		
	
	

	@SuppressWarnings("unchecked")
	public List<User> searchLikeVo(User vo, Pageable pageable) {
		log.debug("UserRepositoryImpl searchWithVo vo: " + vo);
		BooleanBuilder builder = new BooleanBuilder();
		QUser user = QUser.user;

		if ( vo.getCompanyId() != null ) {
			builder.and(user.companyId.eq(vo.getCompanyId())); //java.lang.Long
		}	
		if ( vo.getAuthKey() != null && !"".equals(vo.getAuthKey())) {
			builder.and(user.authKey.containsIgnoreCase(vo.getAuthKey())); //java.lang.String
		}
		if ( vo.getDefaultTaxType() != null && !"".equals(vo.getDefaultTaxType())) {
			builder.and(user.defaultTaxType.containsIgnoreCase(vo.getDefaultTaxType())); //java.lang.String
		}
		if ( vo.getDefaultB2bFlag() != null && !"".equals(vo.getDefaultB2bFlag())) {
			builder.and(user.defaultB2bFlag.containsIgnoreCase(vo.getDefaultB2bFlag())); //java.lang.String
		}
		if ( vo.getPassword() != null && !"".equals(vo.getPassword())) {
			builder.and(user.password.containsIgnoreCase(vo.getPassword())); //java.lang.String
		}
		if ( vo.getUserId() != null ) {
			builder.and(user.userId.eq(vo.getUserId())); //java.lang.Long
		}	
		if ( vo.getRoleId() != null ) {
			builder.and(user.roleId.eq(vo.getRoleId())); //java.lang.Long
		}	
		if ( vo.getName() != null && !"".equals(vo.getName())) {
			builder.and(user.name.containsIgnoreCase(vo.getName())); //java.lang.String
		}
		if ( vo.getCreatorId() != null ) {
			builder.and(user.creatorId.eq(vo.getCreatorId())); //java.lang.Long
		}	
		if ( vo.getModifierId() != null ) {
			builder.and(user.modifierId.eq(vo.getModifierId())); //java.lang.Long
		}	
		if ( vo.getLogoutTime() != null ) {
			builder.and(user.logoutTime.eq(vo.getLogoutTime())); //java.lang.Long
		}	
		if ( vo.getPrinterId() != null ) {
			builder.and(user.printerId.eq(vo.getPrinterId())); //java.lang.Long
		}	
		if ( vo.getAuthUrl() != null && !"".equals(vo.getAuthUrl())) {
			builder.and(user.authUrl.containsIgnoreCase(vo.getAuthUrl())); //java.lang.String
		}
		if ( vo.getCreateDate() != null ) {
			builder.and(user.createDate.eq(vo.getCreateDate())); //java.sql.Timestamp
		}	
		if ( vo.getClose() != null ) {
			builder.and(user.close.eq(vo.getClose())); //java.lang.Boolean
		}	
		if ( vo.getModifyDate() != null ) {
			builder.and(user.modifyDate.eq(vo.getModifyDate())); //java.sql.Timestamp
		}	
		if ( vo.getAccount() != null && !"".equals(vo.getAccount())) {
			builder.and(user.account.containsIgnoreCase(vo.getAccount())); //java.lang.String
		}
		if ( vo.getEmail() != null && !"".equals(vo.getEmail())) {
			builder.and(user.email.containsIgnoreCase(vo.getEmail())); //java.lang.String
		}
		log.debug("UserRepositoryImpl searchWithVo predicate  " + builder.getValue());
		return Lists.newArrayList(user2Repository.findAll(builder.getValue(), pageable));
	}	
	
	@SuppressWarnings("unchecked")
	public List<User> searchLikeVo(User vo, int pageOffset, int pageSize) {
		Pageable pageable = new PageRequest(pageOffset, pageSize);
		return searchLikeVo(vo, pageable);
	}
	
}
