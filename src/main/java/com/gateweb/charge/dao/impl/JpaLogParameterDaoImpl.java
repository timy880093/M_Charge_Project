/*
 * $Header: $
 * This java source file is generated by pkliu on Tue Jan 30 14:38:14 CST 2018
 * For more information, please contact pkliu@sysfoundry.com
 */
package com.gateweb.charge.dao.impl; 
import com.gateweb.charge.dao.*; 
import com.gateweb.charge.model.*;    
import com.gateweb.charge.*;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import java.util.Collection;
import javax.persistence.Query;
import java.util.List;
import java.util.ArrayList;
import javax.persistence.*;
import com.gateweb.db.dao.exception.DaoSystemException;
import com.gateweb.jpa.dao.JpaGenericDaoImpl;
import org.springframework.stereotype.Repository;
/**
 * 
 * @author pkliu
 *
 * This class provides methods to populate DB Table of LogParameter
 */
@Repository("logParameter")
public class JpaLogParameterDaoImpl extends JpaGenericDaoImpl<LogParameterEntity,     java.lang.Integer >  implements LogParameterDao{

	/**
	 *
	 */
	public JpaLogParameterDaoImpl(){
	}

	/**
	 *
	 */ 
	protected final Log logger = LogFactory.getLog(getClass());

    /**
     * Delete a record in Database.
	 * @param parameterId   PK 
    */
	public void delete(
						 java.lang.Integer parameterId 
	) {
		log.debug("JpaLogParameterDaoImpl delete  begin "
			+"id="+parameterId
		);	
		try {
			Object data = entityManager.find(LogParameterEntity.class
			, parameterId
			);
			entityManager.remove(data);
		} catch (IllegalStateException ise){
			//	RuntimeException
			ise.printStackTrace();
			throw new DaoSystemException("JPA10001-"+ise.getMessage(),ise);
		} catch (IllegalArgumentException iae){
			//	RuntimeException
			iae.printStackTrace();
			throw new DaoSystemException("JPA10002-"+iae.getMessage(),iae);
		} catch (TransactionRequiredException tre){
			//	PersistenceException	
			tre.printStackTrace();
			throw new DaoSystemException("JPA10004-"+tre.getMessage(),tre);	
		} 
    }  
   
	@SuppressWarnings("unchecked")
	public List<LogParameterEntity> getSome(int pageNo, int noRowsPerPage)
		{
		List<LogParameterEntity> results = new ArrayList();
		Query q;
		try {
			q = entityManager.createQuery("select obj from LogParameterEntity obj ");
		} catch (IllegalStateException ise){
			//	RuntimeException
			ise.printStackTrace();
			throw new DaoSystemException("JPA10001-"+ise.getMessage(),ise);
		} catch (IllegalArgumentException iae){
			//	RuntimeException
			iae.printStackTrace();
			throw new DaoSystemException("JPA10002-"+iae.getMessage(),iae);
		}
		try {
			if (pageNo > 0 && noRowsPerPage > 0) {
				q.setFirstResult(noRowsPerPage * (pageNo - 1) + 1).setMaxResults(
					noRowsPerPage);
				results = q.getResultList();
			}
		} catch (IllegalStateException ise){
			//	RuntimeException
			ise.printStackTrace();
			throw new DaoSystemException("JPA10001-"+ise.getMessage(),ise);
		}
		return results;
	}
	
	@SuppressWarnings("unchecked")
	public List<LogParameterEntity> searchByProperty(String propertyName,
			final Object value, final int... rowStartIdxAndCount) {
		final String queryString = "select model from LogParameterEntity model where model."
					+ propertyName + "= :propertyValue";
		log.debug("JpaLogParameterDaoImpl findByProperty   queryString :   "+queryString);
		try {
			Query query = entityManager.createQuery(queryString);
			query.setParameter("propertyValue", value);
			if (rowStartIdxAndCount != null && rowStartIdxAndCount.length > 0) {
				int rowStartIdx = Math.max(0, rowStartIdxAndCount[0]);
				if (rowStartIdx > 0) {
					query.setFirstResult(rowStartIdx);
				}

				if (rowStartIdxAndCount.length > 1) {
					int rowCount = Math.max(0, rowStartIdxAndCount[1]);
					if (rowCount > 0) {
						query.setMaxResults(rowCount);
					}
				}
			}
			return query.getResultList();
		} catch (IllegalStateException ise){
			//	RuntimeException
			ise.printStackTrace();
			throw new DaoSystemException("JPA10001-"+ise.getMessage(),ise);
		} catch (IllegalArgumentException iae){
			//	RuntimeException
			iae.printStackTrace();
			throw new DaoSystemException("JPA10002-"+iae.getMessage(),iae);
		}

	}   	
	
	@SuppressWarnings("unchecked")
	public List<LogParameterEntity> searchBy(LogParameterEntity data) {
		StringBuffer sb = new StringBuffer("select obj from LogParameterEntity obj where 1=1 ");

		if ( data.getLogPattern() != null ) {
			sb.append(" AND obj.logPattern = :logPattern "); //java.lang.String
		}
		if ( data.getActionName() != null ) {
			sb.append(" AND obj.actionName = :actionName "); //java.lang.String
		}
		if ( data.getParameterId() != null ) {
			sb.append(" AND obj.parameterId = :parameterId "); //java.lang.Integer
		}
		Query q;
		List<LogParameterEntity> results;
		try {
			log.debug("JpaLogParameterDaoImpl searchBy  query : "+sb.toString());
			q = entityManager.createQuery(sb.toString());
		} catch (IllegalStateException ise){
			//	RuntimeException
			ise.printStackTrace();
			throw new DaoSystemException("JPA10001-"+ise.getMessage(),ise);
		} catch (IllegalArgumentException iae){
			//	RuntimeException
			iae.printStackTrace();
			throw new DaoSystemException("JPA10002-"+iae.getMessage(),iae);
		}
		if ( data.getLogPattern() != null ) {
			q.setParameter("logPattern", data.getLogPattern());
		}
		if ( data.getActionName() != null ) {
			q.setParameter("actionName", data.getActionName());
		}
		if ( data.getParameterId() != null ) {
			q.setParameter("parameterId", data.getParameterId());
		}
		try {
			results = q.getResultList();
		} catch (IllegalStateException ise){
			//	RuntimeException
			ise.printStackTrace();
			throw new DaoSystemException("JPA10001-"+ise.getMessage(),ise);
		} 
		return results; 
	}

	@SuppressWarnings("unchecked")
	public List<LogParameterEntity> searchLike(LogParameterEntity data) {
		StringBuffer sb = new StringBuffer("select obj from LogParameterEntity obj where 1=1 ");
		if ( data.getLogPattern() != null && !"".equals(data.getLogPattern())) {
			sb.append(" AND obj.logPattern Like :logPattern "); //java.lang.String
		}
		if ( data.getActionName() != null && !"".equals(data.getActionName())) {
			sb.append(" AND obj.actionName Like :actionName "); //java.lang.String
		}
		if ( data.getParameterId() != null && !"".equals(data.getParameterId())) {
			sb.append(" AND obj.parameterId Like :parameterId "); //java.lang.Integer
		}
		Query q;
		List<LogParameterEntity> results;
		try {
			log.debug("JpaLogParameterDaoImpl searchLike  query : "+sb.toString());
			q = entityManager.createQuery(sb.toString());
		} catch (IllegalStateException ise){
			//	RuntimeException
			ise.printStackTrace();
			throw new DaoSystemException("JPA10001-"+ise.getMessage(),ise);
		} catch (IllegalArgumentException iae){
			//	RuntimeException
			iae.printStackTrace();
			throw new DaoSystemException("JPA10002-"+iae.getMessage(),iae);
		}
		if ( data.getLogPattern() != null && !"".equals(data.getLogPattern()) ) {
			q.setParameter("logPattern", data.getLogPattern()+"%");
		}
		if ( data.getActionName() != null && !"".equals(data.getActionName()) ) {
			q.setParameter("actionName", data.getActionName()+"%");
		}
		if ( data.getParameterId() != null && !"".equals(data.getParameterId()) ) {
			q.setParameter("parameterId", data.getParameterId()+"%");
		}
		try {
			results = q.getResultList();
		} catch (IllegalStateException ise){
			//	RuntimeException
			ise.printStackTrace();
			throw new DaoSystemException("JPA10001-"+ise.getMessage(),ise);
		}
		
		return results;
	}	
	
}
