/*
 * $Header: $
 * This java source file is generated by pkliu on Mon Jan 29 11:19:52 CST 2018
 * For more information, please contact pkliu@sysfoundry.com
 */
package com.gateweb.orm.einv.repository;

import com.gateweb.db.dao.exception.DaoSystemException;
import com.gateweb.orm.einv.entity.EinvIasrEntity;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

/**
 * @author pkliu
 * <p>
 * This class provides methods to populate DB Table of InvoiceAmountSummaryReport
 */
@Repository("einvInvoiceAmountSummaryReportRepository")
public interface InvoiceAmountSummaryReportRepository extends JpaRepository<EinvIasrEntity, Long> {

    void deleteById(
            Long id
    ) throws DaoSystemException;

    @Query(value = "SELECT count(id) FROM invoice_amount_summary_report WHERE id >= ?1 AND id < ?2",
            nativeQuery = true)
    long findCountBetweenId(long from, long to);

    @Query(value = "SELECT max(id) FROM invoice_amount_summary_report", nativeQuery = true)
    long findMaximumId();

    @Query(value = "SELECT min(id) FROM invoice_amount_summary_report", nativeQuery = true)
    long findMinimumId();

    @Query(value = "SELECT count(id) FROM invoice_amount_summary_report", nativeQuery = true)
    long findCountId();

    
}

